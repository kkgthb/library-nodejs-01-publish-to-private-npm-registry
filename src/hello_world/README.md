# @insertyourcompanynamespacehere/hello-world

Ultraminimalist demo package

## Consuming this package

### Find and replace

**IMPORTANT:**  Hopefully, whoever published the package thought to find-and-replace `@insertyourcompanynamespacehere`, so you should, too, when consuming it!

### Example surface

This code:

```js
const sayHello = require("@insertyourcompanynamespacehere/hello-world");
console.log(sayHello("web developer"));
```

Should log:

```text
Hello web developer, from a (hopefully) private NPM feed!
```
